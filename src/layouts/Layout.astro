---
import BaseLayout from '@/layouts/BaseLayout.astro'

export interface Props {
	title: string;
  description?: string;
  og_image?: string;
}

const { title, og_image, description } = Astro.props;
---
<BaseLayout title={title} description={description} og_image={og_image}>
  <slot />
</BaseLayout>
